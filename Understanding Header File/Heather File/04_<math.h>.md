This is use to use mathematical concept in coding

<math.h>

     // --- Square Root and Power ---
    // double x = 9.0;
    // x = sqrt(x);      // sqrt means square root. Returns 3.0
    // x = pow(x, 3);    // power. Raises x to the power of 3 (x^3). Returns 27.0
    
    // --- Rounding Functions ---
    // double x = 3.14;
    // x = round(x);     // Rounds to the nearest whole number (3.14 becomes 3.0, 3.5 becomes 4.0)
    // x = ceil(x);      // Ceiling: Always rounds UP to the next whole number (3.14 becomes 4.0)
    // x = floor(x);     // Floor: Always rounds DOWN to the previous whole number (3.14 becomes 3.0)
    
    // --- Absolute Value ---
    // double x = -4.5;
    // x = fabs(x);      // Absolute value for floating-point numbers (returns 4.5)
    // Note: For integers, use abs() from <stdlib.h>. For math.h, use fabs().
    
    // --- Logarithms ---
    // double x = 2.71828;
    // x = log(x);       // Natural logarithm (ln), base 'e'. log(e) returns 1.0
    // x = log10(x);     // Base-10 logarithm if needed
    
    // --- Trigonometry (SOH CAH TOA) ---
    // double x = 0.523599; // Angle must be in RADIANS, not degrees! (0.523599 rad = 30 degrees)
    // x = sin(x);         // Sine of the angle
    // x = cos(x);         // Cosine of the angle
    // x = tan(x);         // Tangent of the angle
