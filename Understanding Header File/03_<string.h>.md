Heather File the string.h this is commonly use in string

<string.h> 

    strcpn() this one will select a spicific character in the string
    strlen() this one will measure the sring len
    strcpy() this will copy the string
    
