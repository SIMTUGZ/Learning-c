//Arithmetic Operator

#include <stdio.h>

int main() {
   
   // + addition            
   // - subtraction
   // * multiplication
   // / division
   // % remainder
   // ++ increment  +1
   // -- decrement  -1
   
   // x++ (Post-increment) uses the same value before the update.
   // ++x (Pre-increment) updates the value before the check or use.

   // style of creating arithmetic operation 
   int x = 10;
   
   int add1 = x + 1; // long  you can use all of the operation
   int add2 = x+= 1; // short you can use all of the operation
   
   printf("%d \n",add1);
   printf("%d \n",add1);
   
   return 0;
}
