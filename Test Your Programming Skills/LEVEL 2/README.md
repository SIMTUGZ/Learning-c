1. Write a C program that calculates the area of a rectangle.

- The program should ask the user to input the length and width of the rectangle.
- Calculate the area using the formula: Area = Length × Width
- Display the calculated area.

### Sample Input

        Input the Length: 10
        Input the Width: 5
        The Area of the rectangle is 50

---

2. Write a C program that calculates the area and perimeter of a triangle.

- The program should ask the user to input the base and height to calculate the area.
- The program should ask the user to input all three sides (a, b, c) to calculate the perimeter.
- Use the formula: Area = (1/2) × Base × Height
- Use the formula: Perimeter = a + b + c
- Display both the area and perimeter with two decimal places.

### Sample Input

        --Aria of the triangle-- 
        Input Base: 10
        Input height: 8
        
        --Perimeter of the triangle-- 
        Input A: 5
        Input B: 6
        Input C: 7

### Expected Output

        The Area of the triangle is 40.00 
        The Perimeter of the triangle is 18.00 

---

3. Write a C program that calculates the area and circumference of a circle.

- The program should ask the user to input the radius of the circle.
- Calculate the area using the formula: Area = π × R²
- Calculate the circumference using the formula: Circumference = 2 × π × R
- Use π = 3.1415
- Display both the area and circumference.

### Sample Input

        --Aria of a Circle--
        Input Radius: 5

### Expected Output

        The area of the circle is 78.537498 
        The Circumference of the circle is 31.415001 

---

4. Write a C program that converts temperature from Celsius to Fahrenheit.

- The program should ask the user to input a temperature in Celsius.
- Convert the temperature to Fahrenheit using the formula: F = (C × 1.8) + 32
- Display the result with two decimal places.

### Sample Input

        Input Celsius: 25

### Expected Output

        25.00 Celsius is 77.00 Fahrenheit 

---

5. Write a C program that calculates the missing angle in a triangle.

- The program should ask the user to input two angles of a triangle.
- Calculate the missing third angle using the formula: C = 180 - (A + B)
- Display the missing angle with two decimal places.

### Sample Input

        --Angle of a Triangle-- 
        Angle A: 45
        Angle B: 60

### Expected Output

        The angle of the triangle is 75.00 

---

6. Write a C program that performs basic arithmetic operations and demonstrates increment/decrement operations.

- The program should ask the user to input two numbers.
- Perform and display the following operations: Addition, Subtraction, Multiplication, Division, and Modulus (Remainder).
- Then ask the user to input another number and demonstrate increment (++) and decrement (--) operations.
- Display all results clearly.

### Sample Input

        Input First number: 10
        Input Second number: 3
        
        Input number: 5

### Expected Output

        10 + 3 = 13 
        10 - 3 = 7 
        10 * 3 = 30 
        10 / 3 = 3 
        10 % 3 = 1 
        
        Increment 6 
        Decrement 5 

